CREATE TABLE customers (id SERIAL PRIMARY KEY, name VARCHAR(100), email VARCHAR(100), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP); CREATE TABLE analysts (id SERIAL PRIMARY KEY, name VARCHAR(100), department VARCHAR(100), hire_date DATE);